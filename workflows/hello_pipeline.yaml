apiVersion: ai.sap.com/v1alpha1
kind: Workflow
metadata:
  name: hello-pipeline
  annotations:
    scenario.ai.sap.com/name: "hello-scenario"
    scenario.ai.sap.com/description: "Minimal Hello World pipeline for testing"
spec:
  entrypoint: main
  templates:
    - name: main
      container:
        image: alpine:latest
        command: ["sh", "-c"]
        args: ["echo 'Hello from SAP AI Core!'"]

